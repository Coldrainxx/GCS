<UserControl x:Class="GCS.Views.SetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GCS.ViewModels"
             xmlns:views="clr-namespace:GCS.Views"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT NAV (Mandatory Hardware) -->
        <Border Grid.Column="0" Background="#1C2128" BorderBrush="#2D333D" BorderThickness="0,0,1,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="SETUP"
                           Foreground="#39D0D8" FontWeight="Bold" FontSize="12"
                           Margin="16,14,10,10"/>
                <ListBox ItemsSource="{Binding Sections}" SelectedItem="{Binding SelectedSection}"
                         Background="Transparent" BorderThickness="0" Foreground="#E6EDF3">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="16,10"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Foreground" Value="#8B949E"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="B" Background="Transparent"
                                                BorderThickness="3,0,0,0" BorderBrush="Transparent"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="B" Property="Background" Value="#232A33"/>
                                                <Setter Property="Foreground" Value="#E6EDF3"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="B" Property="Background" Value="#232A33"/>
                                                <Setter TargetName="B" Property="BorderBrush" Value="#39D0D8"/>
                                                <Setter Property="Foreground" Value="#E6EDF3"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" FontSize="13"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- CONTENT -->
        <ContentControl Grid.Column="1" Content="{Binding SelectedSection.Content}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:FirmwareViewModel}">
                    <views:FirmwareView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:FlightModesViewModel}">
                    <views:FlightModesView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:RadioCalibrationViewModel}">
                    <views:RadioCalibrationView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:AccelCalibrationViewModel}">
                    <views:AccelCalibrationView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:CompassViewModel}">
                    <views:CompassView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:EscCalibrationViewModel}">
                    <views:EscCalibrationView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:ServoOutputViewModel}">
                    <views:ServoOutputView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:PidTuningViewModel}">
                    <views:PidTuningView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:FailsafeViewModel}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <views:FailsafeTabView/>
                    </ScrollViewer>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:SetupPlaceholderViewModel}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40">
                        <TextBlock Text="{Binding Title}" Foreground="#E6EDF3"
                                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding Message}" Foreground="#8B949E"
                                   FontSize="13" TextWrapping="Wrap" MaxWidth="420"
                                   TextAlignment="Center" Margin="0,12,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</UserControl>
