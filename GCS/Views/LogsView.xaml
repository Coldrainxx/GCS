<UserControl x:Class="GCS.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="480">

    <UserControl.Resources>
        <SolidColorBrush x:Key="CardBg" Color="#1C2128"/>
        <SolidColorBrush x:Key="BorderCol" Color="#2D333D"/>
        <SolidColorBrush x:Key="PrimaryText" Color="#E6EDF3"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#8B949E"/>
        <SolidColorBrush x:Key="MutedText" Color="#5A636D"/>
        <SolidColorBrush x:Key="Accent" Color="#1F6FEB"/>
        <SolidColorBrush x:Key="Warn" Color="#FF9500"/>
        <SolidColorBrush x:Key="Bad" Color="#F85149"/>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.65"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedText}"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SecondaryText}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,14,0,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Log picker -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="FLIGHT LOG" Style="{StaticResource StatLabel}" FontSize="10"/>

            <Grid Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0"
                          ItemsSource="{Binding RecentLogs}"
                          SelectedItem="{Binding SelectedLog}"
                          FontSize="11" Padding="6,4">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="11"/>
                                <TextBlock FontSize="9" Foreground="{StaticResource MutedText}">
                                    <Run Text="{Binding ModifiedText, Mode=OneWay}"/>
                                    <Run Text="·"/>
                                    <Run Text="{Binding SizeText, Mode=OneWay}"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Button Grid.Column="1" Content="Open…" Command="{Binding OpenFileCommand}"
                        Style="{StaticResource FlatButton}" Background="#30363D" Margin="5,0,0,0"/>
                <Button Grid.Column="2" Content="↻" Command="{Binding RefreshCommand}"
                        Style="{StaticResource FlatButton}" Background="#30363D" Margin="4,0,0,0"
                        ToolTip="Rescan the log folder"/>
            </Grid>

            <TextBlock Text="{Binding Status}" Foreground="{StaticResource SecondaryText}"
                       FontSize="9" Margin="0,6,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Empty state -->
        <TextBlock Grid.Row="1" Margin="0,20,0,0"
                   Text="Pick a log above. Its flight path is drawn on the map."
                   Foreground="{StaticResource MutedText}" FontSize="11" TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasSummary}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0"
                      Visibility="{Binding HasSummary, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <!-- Headline figures -->
                <UniformGrid Columns="2">
                    <StackPanel Margin="0,0,10,12">
                        <TextBlock Text="DURATION" Style="{StaticResource StatLabel}"/>
                        <TextBlock Text="{Binding Summary.DurationText}" Style="{StaticResource StatValue}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="ARMED" Style="{StaticResource StatLabel}"/>
                        <TextBlock Text="{Binding ArmedText}" Style="{StaticResource StatValue}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,10,12">
                        <TextBlock Text="DISTANCE FLOWN" Style="{StaticResource StatLabel}"/>
                        <TextBlock Text="{Binding Summary.DistanceText}" Style="{StaticResource StatValue}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="MAX ALTITUDE" Style="{StaticResource StatLabel}"/>
                        <TextBlock Text="{Binding AltitudeText}" Style="{StaticResource StatValue}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,10,12">
                        <TextBlock Text="MAX SPEED" Style="{StaticResource StatLabel}"/>
                        <TextBlock Text="{Binding SpeedText}" Style="{StaticResource StatValue}" FontSize="11"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="VEHICLES" Style="{StaticResource StatLabel}"/>
                        <TextBlock Text="{Binding VehiclesText}" Style="{StaticResource StatValue}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,10,12">
                        <TextBlock Text="BATTERY" Style="{StaticResource StatLabel}"/>
                        <TextBlock Text="{Binding BatteryText}" Style="{StaticResource StatValue}" FontSize="11"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="GPS" Style="{StaticResource StatLabel}"/>
                        <TextBlock Text="{Binding GpsText}" Style="{StaticResource StatValue}" FontSize="11"/>
                    </StackPanel>
                </UniformGrid>

                <TextBlock Foreground="{StaticResource MutedText}" FontSize="9" TextWrapping="Wrap">
                    <Run Text="Path on map:"/>
                    <Run Text="green = armed, blue = on the ground."/>
                </TextBlock>

                <Button Content="Ask the advisor about this flight"
                        Click="AskAdvisor_Click"
                        Style="{StaticResource FlatButton}"
                        HorizontalAlignment="Left" Margin="0,12,0,0"/>

                <!-- Timeline -->
                <TextBlock Text="TIMELINE" Style="{StaticResource SectionHeader}"/>
                <Border Background="{StaticResource CardBg}" CornerRadius="4"
                        BorderBrush="{StaticResource BorderCol}" BorderThickness="1"
                        Padding="10,8" MaxHeight="220">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Events}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding TimestampUtc, StringFormat=HH:mm:ss}"
                                                   Foreground="{StaticResource MutedText}"
                                                   FontSize="9" FontFamily="Consolas"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Kind}"
                                                   FontSize="9" FontWeight="Bold"
                                                   Margin="8,0,8,0" MinWidth="56">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource SecondaryText}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Kind}" Value="Arm">
                                                            <Setter Property="Foreground" Value="{StaticResource Warn}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Kind}" Value="Critical">
                                                            <Setter Property="Foreground" Value="{StaticResource Bad}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Kind}" Value="Error">
                                                            <Setter Property="Foreground" Value="{StaticResource Bad}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Kind}" Value="Emergency">
                                                            <Setter Property="Foreground" Value="{StaticResource Bad}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Grid.Column="2" Text="{Binding Text}"
                                                   Foreground="{StaticResource PrimaryText}"
                                                   FontSize="9" TextWrapping="Wrap"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <!-- Findings -->
                <TextBlock Text="FINDINGS" Style="{StaticResource SectionHeader}"/>
                <Border Background="{StaticResource CardBg}" CornerRadius="4"
                        BorderBrush="{StaticResource BorderCol}" BorderThickness="1"
                        Padding="10,8">
                    <StackPanel>
                        <TextBlock Text="No problems were detected in what was recorded."
                                   Foreground="{StaticResource MutedText}" FontSize="9"
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Findings.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <ItemsControl ItemsSource="{Binding Findings}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Foreground="{StaticResource PrimaryText}"
                                               FontSize="9" TextWrapping="Wrap" Margin="0,0,0,4"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- What the log cannot tell you -->
                <TextBlock Text="NOT IN THIS LOG" Style="{StaticResource SectionHeader}"/>
                <ItemsControl ItemsSource="{Binding Notes}" Margin="0,0,0,16">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Foreground="{StaticResource MutedText}"
                                       FontSize="9" TextWrapping="Wrap" Margin="0,0,0,3"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
