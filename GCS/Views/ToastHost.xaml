<UserControl x:Class="GCS.Views.ToastHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Background="{x:Null}">

    <!-- Toasts stack in the top-right corner; the host itself is click-through
         except over a toast (so it never blocks the UI behind it). -->
    <ItemsControl ItemsSource="{Binding Items}"
                  HorizontalAlignment="Right" VerticalAlignment="Top"
                  Margin="0,60,16,0" Width="320">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Background="#1C2128" BorderThickness="1,1,1,1"
                        BorderBrush="{Binding Color}" CornerRadius="8"
                        Margin="0,0,0,8" Padding="12,10">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="2" Opacity="0.5"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Icon}" Foreground="{Binding Color}"
                                   FontWeight="Bold" FontSize="14" VerticalAlignment="Top" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding Message}" Foreground="#E6EDF3"
                                   FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center"/>
                        <Button Grid.Column="2" Content="✕" FontSize="11" Foreground="#8B949E"
                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                Padding="6,0" VerticalAlignment="Top"
                                Command="{Binding DataContext.DismissCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"/>
                    </Grid>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
